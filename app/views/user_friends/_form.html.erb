<%= form_with(model: user_friend, local: true) do |form| %>
  <% if user_friend.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_friend.errors.count, "error") %> prohibited this user_friend from being saved:</h2>

      <ul>
        <% user_friend.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :email %>
    <%= form.text_field :email %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
